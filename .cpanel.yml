---
deployment:
  tasks:
    - export APPPATH=/home/ergun1/repositories/cPanelDevelopment_v1.0
    - cd $APPPATH
    - echo "Composer test basladi" > deploy-debug.log
    - date >> deploy-debug.log
    - echo "PATH:" >> deploy-debug.log
    - echo $PATH >> deploy-debug.log
    - echo "which php:" >> deploy-debug.log
    - which php >> deploy-debug.log 2>&1 || true
    - echo "which composer:" >> deploy-debug.log
    - which composer >> deploy-debug.log 2>&1 || true
    - echo "ea-php84:" >> deploy-debug.log
    - ls -la /opt/cpanel/ea-php84/root/usr/bin/php >> deploy-debug.log 2>&1 || true
    - echo "composer locations:" >> deploy-debug.log
    - find /opt /usr /home/ergun1 -name composer -type f 2>/dev/null >> deploy-debug.log || true
    - echo "composer.phar locations:" >> deploy-debug.log
    - find /opt /usr /home/ergun1 -name composer.phar -type f 2>/dev/null >> deploy-debug.log || true
    - echo "Test bitti" >> deploy-debug.log
